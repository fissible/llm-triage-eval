*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
# Triage harness: generated eval reports (regenerable). Force-add milestone runs
# with `git add -f` when you want to track progression.
/storage/eval-reports/
# Filament-published assets — regenerated by `composer install` (filament:upgrade)
/public/css
/public/js
/public/fonts
# NOTE: scratch pools in storage/app/triage/*.jsonl (incl. UN-sanitized prod data)
# are already ignored by storage/app/.gitignore — keep it that way.
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
